import React from 'react';
import './start.scss';

const Start = () => {
    return (
        <section id="start">
            <h1>It's a start</h1>
            <h2>Dare to <strike>develop</strike> dream</h2>
        </section>
    );
};

export default Start;
